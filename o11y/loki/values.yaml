loki:
  commonConfig:
    replication_factor: 1
  storage:
    type: 'filesystem'
  auth_enabled: false
  gateway:
    enabled: true
    username: 'admin'
    password: 'admin'
singleBinary:
  replicas: 1